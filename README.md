# AdventOfCode

Each year contains a header file for helper functions, a shell script for initializing workflow and code for each solution.

On Mac, to setup the shell script, enter one of the year's directories and run:
```bash
source setup.sh
```

